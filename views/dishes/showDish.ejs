<!DOCTYPE html>
<html lang="pt">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dish Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" />
  <link rel="icon" type="image/png" href="/favicon.png">
  <link rel="stylesheet" href="/stylesheets/showDish.css" />
</head>

<body>
  <%- include('../partials/header') %>

  <div class="container mt-4">
    <div class="card p-4">
      <% if (!dish) { %>
        <div class="alert alert-warning text-center">
          <h3>Dish Not Found</h3>
        </div>
      <% } else { %>
        <h2 class="text-center mb-4"><%= dish.name %></h2>

        <!--<% if (dish.image) { %>
          <div class="text-center mb-4">
            <img src="/uploads/<%= dish.image %>" alt="<%= dish.name %>" class="img-fluid rounded" style="max-height: 300px;" />
          </div>
        <% } else { %>
          <p class="text-center"><em>No image available for this dish.</em></p>
        <% } %>-->

        <div class="mb-3">
          <p><strong>Description:</strong> <%= dish.description || 'No description available' %></p>
          <p><strong>Category:</strong> <%= dish.category %></p>
          <p><strong>Ingredients:</strong> <%= dish.ingredients && dish.ingredients.length > 0 ? dish.ingredients.join(', ') : 'Not specified' %></p>
          <p><strong>Prices:</strong>
            Small: €<%= dish.prices.pequena?.toFixed(2) || 'N/A' %>,
            Medium: €<%= dish.prices.media?.toFixed(2) || 'N/A' %>,
            Large: €<%= dish.prices.grande?.toFixed(2) || 'N/A' %>
          </p>
          <p><strong>Calories:</strong> <%= dish.nutrition?.calories || 'N/A' %> kcal</p>
          <p><strong>Fat:</strong> <%= dish.nutrition?.fat || 'N/A' %> g</p>
          <p><strong>Protein:</strong> <%= dish.nutrition?.protein || 'N/A' %> g</p>
          <p><strong>Carbohydrates:</strong> <%= dish.nutrition?.carbs || 'N/A' %> g</p>
          <p><strong>NutriScore:</strong> <%= dish.nutriScore || 'N/A' %></p>
          <p><strong>Allergens:</strong> <%= Array.isArray(dish.allergens) && dish.allergens.length > 0 ? dish.allergens.join(', ') : 'None' %></p>
        </div>

        <div class="text-center mt-4">
          <a href="/dishes/showDishes" class="btn btn-secondary">Back to Dishes List</a>
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
