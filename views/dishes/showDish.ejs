<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/showDish.css" />
    <title>Detalhes do Prato</title>
  </head>
  <body>
    <div class="header bg-primary text-white py-3">
      <div class="container d-flex justify-content-between">
        <h3>Detalhes do Prato</h3>
        <a href="/auth/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>

    <div class="container mt-5">
      <% if (!dish) { %>
        <div class="alert alert-danger text-center">
          <h3>Prato não encontrado</h3>
        </div>
      <% } else { %>
        <div class="dish-details card p-4 shadow-sm">
          <h2 class="text-center mb-4"><%= dish.name %></h2>
          <p><strong>Descrição:</strong> <%= dish.description || 'Sem descrição disponível' %></p>
          <p><strong>Preço:</strong> €<%= dish.price.toFixed(2) %></p>
          <p><strong>Categoria:</strong> <%= dish.category %></p>
          <p><strong>Ingredientes:</strong> <%= dish.ingredients.join(', ') %></p>
        </div>
      <% } %>
    </div>

    <div class="text-center mt-4">
      <a href="/dishes/showDishes" class="btn btn-secondary">Voltar à Lista de Pratos</a>
    </div>
  </body>
</html>
