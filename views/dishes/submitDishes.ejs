<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/submitDishes.css" />
    <title>Criar Prato</title>
  </head>
  <body>
    <div class="header bg-primary text-white py-3">
      <div class="container d-flex justify-content-between">
        <h3>Criar Prato</h3>
        <a href="/auth/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>

    <div class="container mt-5">
      <div class="card p-4 shadow-sm">
        <h2 class="text-center mb-4">Novo Prato</h2>
        <form method="post" action="/dishes/submittedDish">
          <div class="mb-3">
            <label for="name" class="form-label">Nome do Prato</label>
            <input
              type="text"
              id="name"
              name="name"
              class="form-control"
              required
            />
          </div>
          <div class="mb-3">
            <label for="description" class="form-label">Descrição</label>
            <textarea
              id="description"
              name="description"
              class="form-control"
              required
            ></textarea>
          </div>
          <div class="mb-3">
            <label for="category" class="form-label">Categoria</label>
            <select id="category" name="category" class="form-select" required>
              <option value="carne">Carne</option>
              <option value="peixe">Peixe</option>
              <option value="sobremesa">Sobremesa</option>
              <option value="vegetariano">Vegetariano</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="ingredients" class="form-label"
              >Ingredientes (separados por vírgula)</label
            >
            <input
              type="text"
              id="ingredients"
              name="ingredients"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="prices_pequena" class="form-label">Preço Pequeno:</label>
            <input type="number" step="0.01" id="prices_pequena" name="prices[pequena]" class="form-control" required>
        </div>
        
        <div class="mb-3">
            <label for="prices_media" class="form-label">Preço Médio:</label>
            <input type="number" step="0.01" id="prices_media" name="prices[media]" class="form-control" required>
        </div>
        
        <div class="mb-3">
            <label for="prices_grande" class="form-label">Preço Grande:</label>
            <input type="number" step="0.01" id="prices_grande" name="prices[grande]" class="form-control" required>
        </div>

        <div class="mb-3">
          <label for="allergens" class="form-label">Alergénios (separados por vírgula):</label>
          <input type="text" id="allergens" name="allergens" class="form-control" />
        </div>

          <button type="submit" class="btn btn-primary w-100">Criar Prato</button>
        </form>
        <div class="text-center mt-4">
          <a href="/index" class="btn btn-secondary w-100">Voltar</a>
        </div>
      </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
