<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/submitDishes.css" />
    <title>Create Dish</title>
  </head>
  <body>
    <div class="header bg-primary text-white py-3">
      <div class="container d-flex justify-content-between">
        <h3>Create Dish</h3>
        <a href="/auth/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>

    <div class="container mt-5">
      <div class="card p-4 shadow-sm">
        <h2 class="text-center mb-4">New Dish</h2>
        <form method="post" action="/dishes/submittedDish">
          <div class="mb-3">
            <label for="name" class="form-label">Dish Name</label>
            <input
              type="text"
              id="name"
              name="name"
              class="form-control"
              required
            />
          </div>
          <div class="mb-3">
            <label for="description" class="form-label">Description</label>
            <textarea
              id="description"
              name="description"
              class="form-control"
              required
            ></textarea>
          </div>
          <div class="mb-3">
            <label for="category" class="form-label">Category</label>
            <select id="category" name="category" class="form-select" required>
              <option value="carne">Meat</option>
              <option value="peixe">Fish</option>
              <option value="sobremesa">Dessert</option>
              <option value="vegetariano">Vegetarian</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="ingredients" class="form-label"
              >Ingredients (comma-separated)</label
            >
            <input
              type="text"
              id="ingredients"
              name="ingredients"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="prices_pequena" class="form-label">Small Price:</label>
            <input
              type="number"
              step="0.01"
              id="prices_pequena"
              name="prices[pequena]"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="prices_media" class="form-label">Medium Price:</label>
            <input
              type="number"
              step="0.01"
              id="prices_media"
              name="prices[media]"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="prices_grande" class="form-label">Large Price:</label>
            <input
              type="number"
              step="0.01"
              id="prices_grande"
              name="prices[grande]"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="allergens" class="form-label"
              >Allergens (comma-separated):</label
            >
            <input
              type="text"
              id="allergens"
              name="allergens"
              class="form-control"
            />
          </div>

          <button type="submit" class="btn btn-primary w-100">
            Create Dish
          </button>
        </form>
        <div class="text-center mt-4">
          <a href="/dashboard" class="btn btn-secondary w-100">Back</a>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
