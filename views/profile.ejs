<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/profile.css">
    <title>Perfil</title>
  </head>
  <body>
    <div class="header">
      <span>Bem-vindo, <%= user.name %></span>
      <a href="/auth/logout" class="btn btn-secondary">Logout</a>
    </div>

    <div class="profile-container">
      <h2>Perfil do <%= user.name %></h2>
      <p><strong>Email:</strong> <%= user.email %></p>
      <p><strong>Função:</strong> <%= user.role === 'cliente' ? 'Cliente' : 'Manager' %></p>

      <% if (user.role === 'cliente') { %>
        <div class="section-title">Informações de Cliente</div>
        <p><strong>Telemóvel:</strong> <%= user.clienteTelemovel || 'Não fornecido' %></p>
        <p><strong>NIF:</strong> <%= user.clienteNif || 'Não fornecido' %></p>
        <p><strong>Endereço:</strong> <%= user.address || 'Não fornecido' %></p>
      <% } else if (user.role === 'manager') { %>
        <div class="section-title">Informações de Manager</div>
        <p><strong>telemóvel:</strong> <%= user.managerTelemovel || 'Não fornecido' %></p>
        <p><strong>NIF:</strong> <%= user.managerNif || 'Não fornecido' %></p>
      <% } %>

      <div class="btn-container">
        <a href="/" class="btn btn-primary">Página Principal</a>
      </div>
    </div>
  </body>
</html>
